<UserControl x:Class="Flex.BusinessIntelligence.WingClient.Views.CubeSelectorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="35" d:DesignWidth="400">
    
    <Grid x:Name="LayoutRoot">
        <StackPanel Orientation="Horizontal" Name="Holder">
            <TextBlock Text="Consultar um cubo:" VerticalAlignment="Center" Margin="5,0,5,0" FontSize="16"></TextBlock>
            <ComboBox MinWidth="200" VerticalContentAlignment="Center" ItemsSource="{Binding}" Name="Combo" Height="30">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <Image Source="bi;/Assets/cube32.png" ImageFailed="Image_ImageFailed" Margin="5" Stretch="Uniform" Width="16" Height="16"/>
                            <TextBlock Text="{Binding Description}" VerticalAlignment="Center"></TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
        </StackPanel>
    </Grid>
</UserControl>
