<UserControl x:Class="Wing.Client.Modules.Shell.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wingp="clr-namespace:Wing.Composite.Presentation.Regions;assembly=Wing.Composite.Presentation"
    xmlns:wsdk="clr-namespace:Wing.Client.Sdk;assembly=Wing.Client.Sdk"
    mc:Ignorable="d"
    MinWidth="800" MinHeight="600">

    <Grid x:Name="LayoutRoot" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition/>
            <RowDefinition Height="22"/>
        </Grid.RowDefinitions>
        <Border Style="{StaticResource LightToolbarBorder}">
            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.RowSpan="1" Grid.Row="0">
                <StackPanel Orientation="Horizontal" wingp:RegionManager.RegionName="ShellMainBar">
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" wingp:RegionManager.RegionName="ShellMainBarRight">
                </StackPanel>
            </Grid>
        </Border>
        <Grid Grid.Row="1" x:Name="MainContent" Margin="0" Visibility="Collapsed" wingp:RegionManager.RegionName="ShellMainContent">
        </Grid>
        <Grid Grid.Row="2" Background="{StaticResource LightToolbarBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Pronto" VerticalAlignment="Center" Margin="5" x:Name="StatusText"/>
            <ProgressBar Margin="3,3,3,3" Grid.Column="1" HorizontalAlignment="Stretch" Value="50" Foreground="DarkGreen" x:Name="StatusProgress"/>
        </Grid>
    </Grid>
</UserControl>
